CREATE TABLE IF NOT EXISTS items (
    Id          VARCHAR(50),
    Name        VARCHAR(50),
    Weight      INTEGER,
    Strength    INTEGER,
    Agility     INTEGER,
    Inteligence INTEGER,
    Damage      INTEGER,
    HitPoints   INTEGER,
    Defense     INTEGER,
    Type        VARCHAR(50)
);